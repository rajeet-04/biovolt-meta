import { create } from 'zustand'
import type { BiomassPoint, CalibrationDraft } from '../types/calibration'
const empty: CalibrationDraft = { name: '', description: '', load_resistance_ohm: 1000, ads1115_offset_mv: 0, optical_dark_raw: null, optical_blank_raw: null, biomass_points: [], reactor_volume_l: null, co2_per_dry_biomass_g_per_g: null, source_notes: '' }
interface WizardStore { draft: CalibrationDraft; deviceId: string; cellId: string; setDraft: (draft: Partial<CalibrationDraft>) => void; addPoint: (point: BiomassPoint) => void; removePoint: (index: number) => void; setSource: (deviceId: string, cellId: string) => void; reset: () => void }
export const useCalibrationWizardStore = create<WizardStore>((set) => ({ draft: empty, deviceId: '', cellId: '', setDraft: (draft) => set((state) => ({ draft: { ...state.draft, ...draft } })), addPoint: (point) => set((state) => ({ draft: { ...state.draft, biomass_points: [...state.draft.biomass_points, point] } })), removePoint: (index) => set((state) => ({ draft: { ...state.draft, biomass_points: state.draft.biomass_points.filter((_, i) => i !== index) } })), setSource: (deviceId, cellId) => set({ deviceId, cellId }), reset: () => set({ draft: { ...empty, biomass_points: [] }, deviceId: '', cellId: '' }) }))
