import type { CalibrationProvenance } from '../../types/analytics'
export function CalibrationProvenance({ provenance }: { provenance: CalibrationProvenance | null }) { return <section aria-labelledby="provenance-heading" className="rounded border border-bio-border bg-bio-panel p-4"><h2 className="font-semibold text-bio-text" id="provenance-heading">Calibration provenance</h2><dl className="mt-2 grid gap-1 text-sm text-bio-muted"><div><dt className="inline">Revision: </dt><dd className="inline">{provenance?.calibration_revision_id ?? 'Unavailable'}</dd></div><div><dt className="inline">Baseline sequence: </dt><dd className="inline">{provenance?.baseline_sequence ?? 'Unavailable'}</dd></div><div><dt className="inline">Baseline time: </dt><dd className="inline">{provenance?.baseline_timestamp ?? 'Unavailable'}</dd></div></dl></section> }
