import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
export function EnergyComparisonChart({ passive, adaptive }: { passive: number | null; adaptive: number | null }) { const data = [{ arm: 'Passive', energy: passive }, { arm: 'Adaptive', energy: adaptive }]; return <section aria-label="Matched energy comparison" className="rounded border border-bio-border bg-bio-panel p-4"><h2 className="font-semibold text-bio-text">Matched energy comparison</h2><p className="text-xs text-bio-muted">Energy (mJ); unavailable values are not plotted.</p><div className="mt-3 h-48"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><XAxis dataKey="arm" /><YAxis unit=" mJ" /><Tooltip /><Bar dataKey="energy" fill="#38bdf8" /></BarChart></ResponsiveContainer></div></section> }
