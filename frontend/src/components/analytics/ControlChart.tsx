import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { AnalyticsSeriesPoint } from '../../types/analytics'
export function ControlChart({ points }: { points: AnalyticsSeriesPoint[] }) { return <section aria-label="Control drivers" className="rounded border border-bio-border bg-bio-panel p-4"><h2 className="font-semibold text-bio-text">Control drivers</h2><p className="text-xs text-bio-muted">Grow LED PWM (0–255), separate from electrical power.</p><div className="mt-3 h-64"><ResponsiveContainer width="100%" height="100%"><LineChart data={points}><XAxis dataKey="elapsed_s" unit=" s" /><YAxis domain={[0, 255]} unit=" PWM" /><Tooltip /><Line dataKey="passive_led_pwm" name="Passive PWM" connectNulls={false} dot={false} stroke="#f59e0b" /><Line dataKey="adaptive_led_pwm" name="Adaptive PWM" connectNulls={false} dot={false} stroke="#a78bfa" /></LineChart></ResponsiveContainer></div></section> }
