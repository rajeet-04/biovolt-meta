# Offline venue drill

With local services healthy, disconnect upstream internet while keeping the laptop hotspot/LAN active. Confirm ESP32/simulator, FastAPI persistence, local PWA, active experiment controls, and Results/history continue. Only the optional Cloudflared URL should fail. Restore internet and verify the tunnel independently; do not treat tunnel status as local system health.
