# Claim traceability

Every headline claim must link UI field, API field, experiment ID, evidence
class, calibration revision where applicable, and export/raw source. Missing
trace makes the claim unavailable. Historical results remain historical, and
synthetic fallback is labeled `synthetic_demo`.
