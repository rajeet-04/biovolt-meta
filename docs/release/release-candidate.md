# BioVolt release candidate

Freeze the accepted git revision, schema, firmware/backend/frontend versions,
calibration revisions, startup command, environment preparation, and known
MINOR limitations only after `phase9_release_gate.py` reports `PASS`. Keep
tokens, PINs, Wi-Fi credentials, and private attestations outside the evidence
index.
