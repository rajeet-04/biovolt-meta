# Operator journey

Authenticate, inspect device/calibration readiness, select an experiment, run
an allowed mode, observe pending then ACK/rejection and actual telemetry, stop,
and review Results/export. Pending commands are never actuator state; ownership,
preemption, safety interlocks, and the pinned calibration revision stay visible.
