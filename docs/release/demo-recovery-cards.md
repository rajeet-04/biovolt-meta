# Demo recovery cards

For backend restart, ESP32 reconnect, hotspot loss, stale dashboard, invalid
calibration, sensor unavailable, and public-tunnel failure, record symptom,
expected UI state, one or two safe actions, what not to do, and the fallback
level. Local operation should not depend on upstream internet. Never change
evidence class or hide a warning to preserve the narrative.
