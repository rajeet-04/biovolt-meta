# Analytics validation

Recompute passive/adaptive energy from exported raw samples, then compare the
independent result with the API and PWA headline. Use the frozen tolerances in
Phase 9.4; eligibility, evidence class, reason codes, negative gain, and
`Unavailable` semantics must agree exactly.
